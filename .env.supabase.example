# Supabase Environment Variables
# Copy to .env and fill in your Supabase project values

# Frontend (Vite) - add to frontend/.env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Backend (FastAPI) - add to .env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Get these values from:
# 1. Supabase Dashboard > Settings > API
# 2. Project URL = SUPABASE_URL / VITE_SUPABASE_URL
# 3. anon/public = VITE_SUPABASE_ANON_KEY
# 4. service_role = SUPABASE_SERVICE_ROLE_KEY (keep secret!)
# 5. JWT Secret = SUPABASE_JWT_SECRET (Settings > API > JWT Settings)
